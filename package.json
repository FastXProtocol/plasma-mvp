{
  "name": "fastx-sdk",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "axios": "^0.18.0",
    "rlp": "^2.0.0",
    "web3": "^1.0.0-beta.34"
  },
  "scripts": {
    "build": "babel plasma_js_client -d plasma_js_client_dist",
    "test": "npm run build && node plasma_js_client_dist/test"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  }
}
